version: "3"
services:
  fastapi-app:
    build: .
    command: uvicorn main:app --host 0.0.0.0
    volumes:
      - .:/app/app
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"


    



